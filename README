Steps to regenerate gRPC files:
1. go install google.golang.org/protobuf/cmd/protoc-gen-go@latest
2. go install google.golang.org/grpc/cmd/protoc-gen-go-grpc@latest
3. cd /path/to/cavetrack-caves
4. `protoc --go_out=./internal --go_opt=paths=source_relative --go-grpc_out=./internal --go-grpc_opt=paths=source_relative ./pb/caves_service.proto`

--go_out specifies the path for the caves_service.pb.go file. (defines request, response, message types)
--go-grpc_out specifies the path for the caves_service_grpc.pb.go file. (defines interfaces for us to call to interact with request, response, message types)
./pb/caves_service.proto specifies the path to the protobuf specification file

Note. I am unsure why ./internal is used instead of ./internal/pb (the path where the files are generated)